@using FinalMockIdentityXCountry.Models.ViewModels.CoachAreaViewModels.DataController
@model ViewDataEnteredViewModel

<label>@Model.RunnerName | @Model.PracticeLocation | @Model.PracticeStartDateTime</label>

@if (Model.ViewDataEnteredViewModelHelpers.Count() > 0)
{
    <table class="table">
        <thead>
            <tr>
                <th scope="col">Workout</th>
                <th scope="col">Pace</th>
                <th scope="col">Distance</th>
                <th scope="col"></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var workoutData in Model.ViewDataEnteredViewModelHelpers)
            {
                <tr>
                    <td>@workoutData?.WorkoutName</td>
                    <td>@workoutData?.Pace</td>
                    <td>@workoutData?.Distance</td>
                    <td><a asp-action="EditEnteredData" asp-route-workoutInformationId="@workoutData?.WorkoutId" class="btn btn-link">Edit Data</a></td>
                </tr>
            }
        </tbody>
    </table>
}
