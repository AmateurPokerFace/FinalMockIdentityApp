@using FinalMockIdentityXCountry.Models.ViewModels.CoachAreaViewModels
@model NewForumThreadViewModel

<div class="form-control">
<form method="post">
    <div>
        <label asp-for="ThreadTitle" class="form-label"></label>
        <input asp-for="ThreadTitle" class="form-control"/>
    </div>
        <div>
            <label asp-for="ThreadBody" class="form-label"></label>
            <textarea asp-for="ThreadBody" class="form-text" draggable="auto" ></textarea>
        </div>
        <button type="submit" class="btn btn-primary">Submit</button>
    <a class="btn btn-danger">cancel</a>
</form>
</div>